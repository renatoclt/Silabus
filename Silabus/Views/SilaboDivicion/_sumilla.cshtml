
@model Silabus.Models.SilaboDivicion
<!DOCTYPE html>
@if (Model.Divicion.Estado == 1)
{
    @Html.DisplayFor(model => model.Silabo.Sumilla)
}
else
{
    @Html.EditorFor(model => model.Silabo.Sumilla)
}
