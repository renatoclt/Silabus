@model IEnumerable<Silabus.Models.SilaboDocentes>

@{
    Layout = null;
    Layout = "~/Views/Shared/_LayoutSilabo.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Silabos.Asignaturas.Codigo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Silabos.Asignaturas.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Silabos.Sumilla)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Silabos.Asignaturas.Codigo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Silabos.Asignaturas.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Silabos.Sumilla)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id= item.Silabos.Id }) 
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
